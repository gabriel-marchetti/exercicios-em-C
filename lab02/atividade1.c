#include <stdio.h>


void createMatrix(int )

int main(){
    // Vou indicar um quadrado seguro com S.
    // Vou indicar um quadrado perigoso com P.
    int line, column;

    scanf("%dx%d", &line, &column);
    // Preciso criar a matriz do campo de batalha.


    return(0);
}